<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="backup">
	<select id="getAdmins" resultType="Admins">
		SELECT * FROM ADMINS ORDER BY ADMINS_SQ
	</select>
	<select id="getMonths" resultType="int">
		SELECT * FROM MONTHS ORDER BY MON
	</select>
	<select id="getYears" resultType="int">
		SELECT * FROM YEARS ORDER BY YR
	</select>
	<select id="getVerses" resultType="Verses">
		SELECT * FROM VERSES ORDER BY VERSES_SQ
	</select>
	<select id="getUpdates" resultType="Update2">
		SELECT * FROM UPDATEDATES ORDER BY UPDATEDATE_SQ
	</select>
	
</mapper>